# Dependencies for headless GitHub Actions workflows (scheduled scans,
# grading reports, and LightGBM batch training) that run `scripts/*.py`
# against `modules/` directly and never render the Streamlit UI.
#
# Deliberately excludes UI-only packages from requirements.txt (`streamlit`,
# `plotly`, `Pillow`): every `modules/` file that optionally uses Streamlit
# (`import streamlit as st` for `st.cache_data`) already falls back to a
# local TTL-aware cache when Streamlit isn't installed, so these workflows
# don't need it. See `requirements.txt` for the full Streamlit dashboard set.
pandas==3.0.5
numpy==2.5.3
ta==0.11.0
transformers==5.17.0
torch==2.14.0
requests==2.34.2
python-dotenv==1.2.3
beautifulsoup4==4.13.4
lxml==6.1.0
statsmodels==0.15.0
scikit-learn==1.9.1
lightgbm==4.7.0
arch==8.0.0
